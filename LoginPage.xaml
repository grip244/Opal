<Page
    x:Class="Opal.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Opal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid x:Name="RootGrid" Background="{ThemeResource AppBackgroundBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DeviceStates">
                <VisualState x:Name="XboxState">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Padding" Value="48,40,48,48" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Notification Toast -->
        <muxc:TeachingTip x:Name="ErrorToast"
            Title="Connection Error"
            Subtitle="Authentication failed. Please check your credentials."
            PreferredPlacement="Top"
            PlacementMargin="20"
            IsLightDismissEnabled="True"
            CloseButtonContent="Close">
            <muxc:TeachingTip.IconSource>
                <muxc:SymbolIconSource Symbol="Stop" />
            </muxc:TeachingTip.IconSource>
        </muxc:TeachingTip>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="600" Spacing="16" Height="Auto">
            <TextBlock Text="Opal" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,12" Foreground="#4ECCA3"/>
            <TextBlock Text="Connect to your Music" FontSize="16" HorizontalAlignment="Center" Margin="0,-20,0,8" Opacity="0.8" Foreground="{ThemeResource AppTextForegroundBrush}"/>
            <TextBox x:Name="ServerTextBox" Header="Server URL" PlaceholderText="https://navidrome.example" FontSize="8" Height="47" KeyDown="OnLoginFieldKeyDown"/>
            <TextBox x:Name="UsernameTextBox" Header="Username" FontSize="8" Height="47" KeyDown="OnLoginFieldKeyDown"/>
            <PasswordBox x:Name="PasswordBox" Header="Password" FontSize="8" Height="47" KeyDown="OnLoginFieldKeyDown"/>
            <CheckBox x:Name="RememberMeCheckBox" Content="Remember Me" IsChecked="True" FontSize="8" Width="97" Height="12" HorizontalAlignment="Left" RenderTransformOrigin="0.5,0.5" >
                <CheckBox.RenderTransform>
                    <CompositeTransform ScaleX="1"/>
                </CheckBox.RenderTransform>
            </CheckBox>
            <Button x:Name="ConnectButton" Content="Connect" HorizontalAlignment="Stretch" Height="24" FontSize="8" Background="#4ECCA3" Foreground="White" Click="OnConnectClicked" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,8,0,0">
                <HyperlinkButton Content="Privacy Policy" FontSize="12" NavigateUri="https://darademsc.ddns.net/#privacy" Padding="0" />
                <TextBlock Text="•" VerticalAlignment="Center" FontSize="12" Opacity="0.4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                <HyperlinkButton Content="Terms of Service" FontSize="12" NavigateUri="https://darademsc.ddns.net/#tos" Padding="0" />
                <TextBlock Text="•" VerticalAlignment="Center" FontSize="12" Opacity="0.4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                <HyperlinkButton Content="GitHub Repository" FontSize="12" NavigateUri="https://github.com/grip244/Opal" Padding="0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,-10,0,10">
                <ProgressRing x:Name="ProgressRing" IsActive="False" Width="36" Height="36" />
                <TextBlock x:Name="StatusText" FontSize="12" VerticalAlignment="Center" Width="250" Margin="0,0,30,0" TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
