<Page
    x:Class="Opal.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Opal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>
            
            <CollectionViewSource x:Key="AlbumSongsSource" IsSourceGrouped="True" ItemsPath="Items" />
            <x:Double x:Key="AlbumSize">140</x:Double>
            <x:Double x:Key="TitleFontSize">22</x:Double>
            <Thickness x:Key="PagePadding">24</Thickness>
            
            <Style x:Key="AlbumItemStyle" TargetType="GridViewItem">
                <Setter Property="Width" Value="150" />
                <Setter Property="Margin" Value="0,0,10,0" />
            </Style>
            <Style x:Key="XboxAlbumItemStyle" TargetType="GridViewItem">
                <Setter Property="Width" Value="110" />
                <Setter Property="Margin" Value="0,0,8,0" />
            </Style>

            <Style x:Key="HiddenFlipViewButtonsStyle" TargetType="FlipView">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="FlipView">
                            <Grid Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="ScrollingHost" AutomationProperties.AccessibilityView="Raw" HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}" HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}" IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}" IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}" IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}" VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}" VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}" ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Page.Resources>
    
    <Grid x:Name="RootGrid" Padding="{StaticResource PagePadding}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DeviceStates">
                <VisualState x:Name="XboxState">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Padding" Value="48,40,48,48" />
                        <Setter Target="HomeStack.Spacing" Value="24" />
                        <Setter Target="LibraryTitle.FontSize" Value="20" />
                        <Setter Target="NewestTitle.FontSize" Value="20" />
                        <Setter Target="RecentTitle.FontSize" Value="20" />
                        <Setter Target="ExploreGridView.ItemContainerStyle" Value="{StaticResource XboxAlbumItemStyle}" />
                        <Setter Target="NewlyAddedGridView.ItemContainerStyle" Value="{StaticResource XboxAlbumItemStyle}" />
                        <Setter Target="RecentlyReleasedGridView.ItemContainerStyle" Value="{StaticResource XboxAlbumItemStyle}" />
                        <Setter Target="SpotlightHero.Height" Value="180" />
                        <Setter Target="SpotlightHero.Margin" Value="-48,0,-48,12" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0A0A1A" Offset="0" />
                <GradientStop Color="#101025" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid x:Name="PlayerPanel">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>


            <!-- Content Area -->
            <Grid Grid.Row="1">
                <Grid x:Name="BrowseGrid" Visibility="Visible">
                    <!-- HomeGrid, ArtistGrid, AlbumGrid -->
                    <Grid x:Name="HomeGrid" Visibility="Collapsed" d:Visibility="Visible">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel x:Name="HomeStack" Spacing="32" Padding="0,0,0,40">
                                <!-- Spotlight Carousel -->
                                <Grid x:Name="SpotlightHero" Margin="0,0,0,12" Height="200">
                                    <FlipView x:Name="SpotlightCarousel" ItemsSource="{x:Bind LibraryVM.SpotlightSongs, Mode=OneWay}" Background="Transparent" Style="{StaticResource HiddenFlipViewButtonsStyle}">
                                        <FlipView.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Tapped="OnSpotlightTapped" Tag="{Binding}" Background="#11FFFFFF" CornerRadius="12">
                                                    <!-- Stretched Background -->
                                                    <Image Source="{Binding CoverArt}" Stretch="UniformToFill" Opacity="0.4" />
                                                    <Grid Background="#77000000" /> <!-- Text protection overlay -->

                                                    <StackPanel Orientation="Horizontal" Padding="24" Spacing="24" VerticalAlignment="Center">
                                                        <Border CornerRadius="8" Width="150" Height="150" BorderThickness="1" BorderBrush="#22FFFFFF" Background="#11FFFFFF" Canvas.ZIndex="1">
                                                            <Image Source="{Binding CoverArt}" Stretch="UniformToFill" />
                                                        </Border>
                                                        <StackPanel VerticalAlignment="Center" Spacing="4">
                                                            <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" MaxLines="1" TextTrimming="CharacterEllipsis" />
                                                            <TextBlock Text="{Binding Artist}" FontSize="18" Opacity="0.9" Foreground="White" />
                                                            <TextBlock Text="{Binding Album}" FontSize="14" Opacity="0.6" Foreground="White" />
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </FlipView.ItemTemplate>
                                    </FlipView>

                                    <!-- Arrows -->
                                    <Button VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Background="#99111111" Width="42" Height="42" CornerRadius="21" BorderThickness="0" Click="OnCarouselPrev">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE76B;" FontSize="14" Foreground="White" />
                                    </Button>
                                    <Button VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0" Background="#99111111" Width="42" Height="42" CornerRadius="21" BorderThickness="0" Click="OnCarouselNext">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE76C;" FontSize="14" Foreground="White" />
                                    </Button>
                                </Grid>


                                <!-- Explore from your library -->
                                <StackPanel Spacing="12">
                                    <Grid>
                                        <TextBlock x:Name="LibraryTitle" Text="Explore from your library" FontSize="24" FontWeight="Bold" Foreground="White" />
                                    </Grid>
                                    <GridView x:Name="ExploreGridView" ItemClick="OnGenericAlbumClicked" IsItemClickEnabled="True" SelectionMode="None" 
                                              HorizontalAlignment="Stretch" VerticalAlignment="Top" Padding="0,0,0,8"
                                              ItemContainerStyle="{StaticResource AlbumItemStyle}"
                                              ScrollViewer.HorizontalScrollMode="Enabled" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden" 
                                              ItemsSource="{x:Bind LibraryVM.ExploreAlbums, Mode=OneWay}">
                                        <GridView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1" />
                                            </ItemsPanelTemplate>
                                        </GridView.ItemsPanel>
                                        <GridView.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Spacing="6">
                                                    <Border CornerRadius="8" Background="#11FFFFFF" BorderThickness="1" BorderBrush="#22FFFFFF" Height="{StaticResource AlbumSize}" Width="{StaticResource AlbumSize}">
                                                        <Image Source="{Binding CoverArt}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>
                                                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="White"/>
                                                    <TextBlock Text="{Binding Artist}" FontSize="11" Opacity="0.7" TextTrimming="CharacterEllipsis" Foreground="#DDD"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridView.ItemTemplate>
                                    </GridView>
                                </StackPanel>


                                <!-- Newly Added Releases -->
                                <StackPanel Spacing="12">
                                    <Grid>
                                        <TextBlock x:Name="NewestTitle" Text="Newly added releases" FontSize="24" FontWeight="Bold" Foreground="White" />
                                    </Grid>
                                    <GridView x:Name="NewlyAddedGridView" ItemClick="OnGenericAlbumClicked" IsItemClickEnabled="True" SelectionMode="None" 
                                              HorizontalAlignment="Stretch" VerticalAlignment="Top" Padding="0,0,0,8"
                                              ItemContainerStyle="{StaticResource AlbumItemStyle}"
                                              ScrollViewer.HorizontalScrollMode="Enabled" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden" 
                                              ItemsSource="{x:Bind LibraryVM.NewestAlbums, Mode=OneWay}">
                                        <GridView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1" />
                                            </ItemsPanelTemplate>
                                        </GridView.ItemsPanel>
                                        <GridView.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Spacing="6">
                                                    <Border CornerRadius="8" Background="#11FFFFFF" BorderThickness="1" BorderBrush="#22FFFFFF" Height="{StaticResource AlbumSize}" Width="{StaticResource AlbumSize}">
                                                        <Image Source="{Binding CoverArt}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>
                                                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="White"/>
                                                    <TextBlock Text="{Binding Artist}" FontSize="11" Opacity="0.7" TextTrimming="CharacterEllipsis" Foreground="#DDD"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridView.ItemTemplate>
                                    </GridView>
                                </StackPanel>



                                <!-- Recently Released Albums -->
                                <StackPanel Spacing="12">
                                    <Grid>
                                        <TextBlock x:Name="RecentTitle" Text="Recently released" FontSize="24" FontWeight="Bold" Foreground="White" />
                                    </Grid>
                                    <GridView x:Name="RecentlyReleasedGridView" ItemClick="OnGenericAlbumClicked" IsItemClickEnabled="True" SelectionMode="None" 
                                              HorizontalAlignment="Stretch" VerticalAlignment="Top" Padding="0,0,0,8"
                                              ItemContainerStyle="{StaticResource AlbumItemStyle}"
                                              ScrollViewer.HorizontalScrollMode="Enabled" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden" 
                                              ItemsSource="{x:Bind LibraryVM.RecentReleasedAlbums, Mode=OneWay}">
                                        <GridView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="1" />
                                            </ItemsPanelTemplate>
                                        </GridView.ItemsPanel>
                                        <GridView.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Spacing="6">
                                                    <Border CornerRadius="8" Background="#11FFFFFF" BorderThickness="1" BorderBrush="#22FFFFFF" Height="{StaticResource AlbumSize}" Width="{StaticResource AlbumSize}">
                                                        <Image Source="{Binding CoverArt}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>
                                                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="White"/>
                                                    <TextBlock Text="{Binding Artist}" FontSize="11" Opacity="0.7" TextTrimming="CharacterEllipsis" Foreground="#DDD"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridView.ItemTemplate>
                                    </GridView>
                                </StackPanel>

                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="ArtistGrid" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,0,0,16">
                            <Ellipse Width="80" Height="80">
                                <Ellipse.Fill>
                                    <ImageBrush x:Name="ArtistDetailImageBrush" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock x:Name="ArtistDetailName" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
                        </StackPanel>
                        
                        <GridView x:Name="AlbumsGridView" Grid.Row="1" ItemClick="OnAlbumClicked" IsItemClickEnabled="True">
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Width="150" Margin="12">
                                        <Border CornerRadius="8" Width="140" Height="140" Background="#11FFFFFF">
                                            <Image Source="{Binding CoverArt}" Stretch="UniformToFill" />
                                        </Border>
                                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Margin="0,8,0,0" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Year}" FontSize="12" Opacity="0.7" TextAlignment="Center" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </Grid>

                    <Grid x:Name="AlbumGrid" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="24" Margin="0,0,0,16">
                            <Border CornerRadius="8" Background="#22FFFFFF" Height="100" Width="100">
                                <Image x:Name="AlbumDetailImage" Stretch="Uniform" />
                            </Border>
                            <StackPanel VerticalAlignment="Center" Spacing="4">
                                <TextBlock x:Name="AlbumDetailTitle" FontSize="28" FontWeight="Bold" TextWrapping="Wrap" />
                                <TextBlock x:Name="AlbumDetailArtist" FontSize="14" Opacity="0.8" />
                                <Button Content="Play Album" Click="OnPlayAlbumClicked" FontSize="14" Background="{ThemeResource SystemAccentColor}" Foreground="White" CornerRadius="16" Padding="12,4" Margin="0,4,0,0" UseSystemFocusVisuals="True" />
                            </StackPanel>
                        </StackPanel>
                        
                        <ListView x:Name="TracksListView" Grid.Row="1" ItemClick="OnTrackClicked" IsItemClickEnabled="True" ItemsSource="{Binding Source={StaticResource AlbumSongsSource}}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="80" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding TrackNumber}" FontSize="14" VerticalAlignment="Center" Opacity="0.5" />
                                        <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="16" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="2" Text="{Binding Duration}" FontSize="12" Opacity="0.6" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.HeaderTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,12,0,8" />
                                        </DataTemplate>
                                    </GroupStyle.HeaderTemplate>
                                </GroupStyle>
                            </ListView.GroupStyle>
                        </ListView>
                    </Grid>
                </Grid>

                <!-- Full Player View (Lyrics/Art only) -->
                <Grid x:Name="FullPlayerGrid" Visibility="Collapsed" Background="#D1000000">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Button x:Name="CollapseButton" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="40" Background="#33FFFFFF" Foreground="White" Width="48" Height="48" CornerRadius="24" BorderThickness="0" Click="OnCollapseClicked" UseSystemFocusVisuals="True" ToolTipService.ToolTip="Collapse Player">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70D;" FontSize="18" />
                        </Button>
                        <StackPanel x:Name="AlbumArtView" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="32">
                            <Grid Width="360" Height="360">
                                <Border CornerRadius="20" BorderThickness="1" BorderBrush="#22FFFFFF" Background="#11FFFFFF">
                                    <Image x:Name="NowPlayingImage" Stretch="Uniform" Width="360" Height="360" />
                                </Border>
                            </Grid>
                        </StackPanel>

                        <Grid x:Name="LyricsOverlay" Visibility="Collapsed" Padding="60,20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="LyricsOverlayTitle" Text="Lyrics" FontSize="20" FontWeight="Bold" Opacity="0.5" HorizontalAlignment="Center" Margin="0,0,0,24" />
                                <ListView x:Name="LyricsListView" Grid.Row="1" SelectionMode="Single" ItemsSource="{x:Bind LyricsVM.Lines, Mode=OneTime}" SelectedIndex="{x:Bind LyricsVM.ActiveIndex, Mode=TwoWay}" IsItemClickEnabled="False" HorizontalAlignment="Center" Width="700">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Text}" HorizontalAlignment="Center" TextAlignment="Center" FontSize="28" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Grid>
                    </Grid>

                    <!-- Queue -->
                    <Border x:Name="QueuePanel" Grid.Column="1" Background="#12FFFFFF" CornerRadius="16" Padding="24" Margin="24,24,24,24">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="UP NEXT" FontSize="16" FontWeight="Bold" Opacity="0.6" Margin="0,0,0,16" />
                            <ListView x:Name="QueueListView" Grid.Row="1" SelectionMode="None" IsItemClickEnabled="True" ItemClick="OnQueueItemClick" ItemsSource="{x:Bind UpcomingQueue, Mode=OneWay}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border CornerRadius="6" Background="#11FFFFFF" Grid.Column="0" Width="50" Height="50">
                                                <Image Source="{Binding CoverArt}" Stretch="UniformToFill" />
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                                                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Artist}" FontSize="14" Opacity="0.7" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>


