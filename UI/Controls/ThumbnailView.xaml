<UserControl
    x:Class="Opal.UI.Controls.ThumbnailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Opal.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300">

    <Grid x:Name="RootGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LoadingStates">
                <VisualState x:Name="Loading">
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="Placeholder" Storyboard.TargetProperty="Opacity" To="1" Duration="0" />
                        <DoubleAnimation Storyboard.TargetName="ContentGrid" Storyboard.TargetProperty="Opacity" To="0" Duration="0" />
                    </Storyboard>
                </VisualState>
                <VisualState x:Name="Loaded">
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="Placeholder" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.4" />
                        <DoubleAnimation Storyboard.TargetName="ContentGrid" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.4" />
                    </Storyboard>
                </VisualState>
                <VisualState x:Name="Error">
                    <VisualState.Setters>
                        <Setter Target="Placeholder.Visibility" Value="Collapsed" />
                        <Setter Target="ErrorIcon.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Placeholder / Skeleton -->
        <Rectangle x:Name="Placeholder" Opacity="1">
            <Rectangle.Resources>
                 <Storyboard x:Key="ShimmerAnim" RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="ShimmerTransform" 
                                     Storyboard.TargetProperty="TranslateX"
                                     From="-1" To="1" Duration="0:0:1.5" EnableDependentAnimation="True" />
                </Storyboard>
            </Rectangle.Resources>
            <Rectangle.Fill>
                <LinearGradientBrush x:Name="ShimmerGradient" StartPoint="0,0.5" EndPoint="1,0.5">
                    <GradientStop Color="#11FFFFFF" Offset="0" />
                    <GradientStop Color="#22FFFFFF" Offset="0.5" />
                    <GradientStop Color="#11FFFFFF" Offset="1" />
                    <LinearGradientBrush.RelativeTransform>
                        <CompositeTransform x:Name="ShimmerTransform" />
                    </LinearGradientBrush.RelativeTransform>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Fallback Icon -->
        <Viewbox x:Name="ErrorIcon" Visibility="Collapsed" Width="48" Height="48" Opacity="0.3">
            <SymbolIcon Symbol="Pictures" Foreground="White" />
        </Viewbox>

        <!-- Hardware Accelerated Melted Background -->
        <Canvas x:Name="BackgroundCanvas" Opacity="1.0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

        <!-- Foreground Content -->
        <Grid x:Name="ContentGrid" Opacity="0">
            <Image x:Name="MainImage" 
                   Stretch="UniformToFill" 
                   ImageOpened="OnImageOpened"
                   ImageFailed="OnImageFailed" />
        </Grid>
    </Grid>
</UserControl>
