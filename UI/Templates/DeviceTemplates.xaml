<!-- Path: Opal/UI/Templates/DeviceTemplates.xaml -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:remote="using:Opal"
    xmlns:converters="using:Opal.Converters">

    <converters:RemoteSystemKindToIconConverter x:Key="KindToIconConverter"/>
    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

    <DataTemplate x:Key="RemoteDeviceTemplate">
        <!-- XYFocus Properties for Xbox Controller Navigation -->
        <Grid Background="Transparent" 
              Padding="12,8"
              Width="280"
              XYFocusKeyboardNavigation="Enabled"
              XYFocusUpNavigationStrategy="NavigationDirectionDistance"
              XYFocusDownNavigationStrategy="NavigationDirectionDistance">
            
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <Storyboard>
                            <ColorAnimation Storyboard.TargetName="RootGrid" 
                                            Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)"
                                            To="{ThemeResource SystemAccentColor}" Duration="0:0:0.1" />
                        </Storyboard>
                    </VisualState>
                    <VisualState x:Name="Pressed" />
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <Grid x:Name="RootGrid" CornerRadius="8" Padding="12" Background="#11FFFFFF">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <!-- Icon Layer: Distinguished by RemoteSystemKind -->
                    <FontIcon Glyph="{Binding DeviceType, Converter={StaticResource KindToIconConverter}}" 
                              FontSize="24" 
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource SystemAccentColorLight3}"/>
                    
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding DisplayName}" 
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   FontWeight="SemiBold"
                                   MaxLines="1"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="White"/>
                        <TextBlock Text="{Binding IPAddress}" 
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="#99FFFFFF"/>
                    </StackPanel>
                    
                    <FontIcon Glyph="&#xE73E;" 
                              HorizontalAlignment="Right" 
                              FontSize="12" 
                              VerticalAlignment="Center"
                              Visibility="{Binding IsConnected, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"
                              Foreground="Cyan"/>
                </StackPanel>
            </Grid>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
