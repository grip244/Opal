<Page
    x:Class="Opal.PlaylistDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Opal"
    xmlns:controls="using:Opal.UI.Controls"
    xmlns:converters="using:Opal.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:ExplicitStatusConverter x:Key="ExplicitStatusConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Grid Background="{ThemeResource AppBackgroundBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DeviceStates">
                <VisualState x:Name="XboxState">
                    <VisualState.Setters>
                        <Setter Target="HeaderGrid.Padding" Value="48,40,48,32" />
                        <Setter Target="PlaylistCoverBorder.Width" Value="150" />
                        <Setter Target="PlaylistCoverBorder.Height" Value="150" />
                        <Setter Target="PlaylistTitle.FontSize" Value="36" />
                        <Setter Target="GenreCol.Width" Value="0" />
                        <Setter Target="YearCol.Width" Value="0" />
                        <Setter Target="GenreHeaderCol.Width" Value="0" />
                        <Setter Target="YearHeaderCol.Width" Value="0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid x:Name="HeaderGrid" Grid.Row="0" Padding="32,40,32,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Click="OnBackClicked" Style="{StaticResource NavigationBackButtonNormalStyle}" VerticalAlignment="Top" HorizontalAlignment="Left" Canvas.ZIndex="10"/>

            <Border x:Name="PlaylistCoverBorder" Width="230" Height="230" CornerRadius="12" Margin="0,0,32,0" Background="{ThemeResource AppControlBackgroundBrush}">
                <controls:ThumbnailView x:Name="PlaylistCover" />
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Spacing="8">
                <TextBlock Text="PLAYLIST" FontSize="14" FontWeight="Bold" Opacity="0.6" />
                <TextBlock x:Name="PlaylistTitle" FontSize="48" FontWeight="Bold" Text="Playlist Name" Margin="0,-4,0,0" />
                
                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,16,0,0">
                    <Button Click="OnPlayAllClicked" Background="{ThemeResource SystemAccentColor}" Foreground="White" Width="100" Height="42" CornerRadius="21" Padding="0" UseSystemFocusVisuals="True">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Play" Foreground="White" />
                            <TextBlock Text="Play" FontWeight="Bold" />
                        </StackPanel>
                    </Button>
                    <Button Click="OnShuffleClicked" Background="Transparent" BorderBrush="{ThemeResource AppControlBorderBrush}" BorderThickness="1" Width="120" Height="42" CornerRadius="21" Padding="0" UseSystemFocusVisuals="True">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Shuffle" />
                            <TextBlock Text="Shuffle" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                    <Button Click="OnQueueAllClicked" Background="Transparent" BorderBrush="{ThemeResource AppControlBorderBrush}" BorderThickness="1" Width="130" Height="42" CornerRadius="21" Padding="0" UseSystemFocusVisuals="True">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="List" />
                            <TextBlock Text="Queue All" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Track List -->
        <ListView x:Name="PlaylistSongsListView" 
                  Grid.Row="1"
                  ItemsSource="{x:Bind Songs}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="OnTrackClicked"
                  CanDragItems="True"
                  CanReorderItems="True"
                  AllowDrop="True"
                  DragItemsCompleted="OnDragItemsCompleted"
                  UseSystemFocusVisuals="True">
                <ListView.ItemContainerTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromVerticalOffset="20" />
                    </TransitionCollection>
                </ListView.ItemContainerTransitions>
            
            <ListView.Header>
                <Grid Padding="12,0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition x:Name="GenreHeaderCol" Width="150" />
                        <ColumnDefinition x:Name="YearHeaderCol" Width="80" />
                        <ColumnDefinition Width="60" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="1" Text="#" FontWeight="SemiBold" Opacity="0.4" FontSize="11" HorizontalAlignment="Center" />
                    <TextBlock Grid.Column="2" Text="TITLE" FontWeight="SemiBold" Opacity="0.4" FontSize="11" Margin="60,0,12,0" />
                    <FontIcon Grid.Column="3" FontFamily="Segoe MDL2 Assets" Glyph="&#xE916;" Opacity="0.4" FontSize="12" HorizontalAlignment="Center" />
                    <TextBlock Grid.Column="4" Text="ALBUM" FontWeight="SemiBold" Opacity="0.4" FontSize="11" Margin="12,0" />
                    <TextBlock Grid.Column="5" Text="GENRE" FontWeight="SemiBold" Opacity="0.4" FontSize="11" Margin="12,0" />
                    <TextBlock Grid.Column="6" Text="YEAR" FontWeight="SemiBold" Opacity="0.4" FontSize="11" Margin="12,0" HorizontalAlignment="Right" />
                    <FontIcon Grid.Column="7" FontFamily="Segoe MDL2 Assets" Glyph="&#xEB51;" Opacity="0.4" FontSize="12" HorizontalAlignment="Center" />
                </Grid>
            </ListView.Header>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,2" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Song">
                    <Grid Height="64" Padding="12,0" Background="Transparent" Name="TrackGrid">
                        <Grid.ContextFlyout>
                            <MenuFlyout Opening="OnTrackContextOpening">
                                <MenuFlyoutItem Text="Play" Click="OnPlayMenuClicked" Icon="Play" />
                                <MenuFlyoutItem Text="Add to Queue" Click="OnAddQueueMenuClicked" Icon="Add" />
                                <MenuFlyoutSeparator />
                                <MenuFlyoutItem Name="RemoveFromPlaylistMenu" Text="Remove from this Playlist" Click="OnRemoveMenuClicked" Icon="Delete" />
                            </MenuFlyout>
                        </Grid.ContextFlyout>

                        <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition x:Name="GenreCol" Width="150" />
                        <ColumnDefinition x:Name="YearCol" Width="80" />
                        <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        
                        <Border Width="4" Height="24" CornerRadius="2" Background="{ThemeResource SystemAccentColor}" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="{Binding IsPlaying, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="-12,0,0,0" />
 
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE700;" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.3" FontSize="12" />

                        <TextBlock Grid.Column="1" Text="{x:Bind PlaylistIndex, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.4" FontSize="13" />
                        
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="44" Height="44" CornerRadius="6" Background="{ThemeResource AppControlBackgroundBrush}">
                                <controls:ThumbnailView SourceUrl="{x:Bind CoverUrl}" />
                            </Border>
                            <StackPanel VerticalAlignment="Center" Margin="16,0">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{ThemeResource SystemAccentColor}" Visibility="{Binding IsPlaying, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,4,0" />
                                    <TextBlock Text="{x:Bind Title}" MaxLines="1" TextTrimming="CharacterEllipsis" FontSize="15" FontWeight="SemiBold" Foreground="{ThemeResource AppTextForegroundBrush}" />
                                    <Border Margin="6,0,0,0" Background="{ThemeResource AppSecondaryTextForegroundBrush}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Visibility="{x:Bind ExplicitStatus, Converter={StaticResource ExplicitStatusConverter}}">
                                        <TextBlock Text="E" FontSize="10" FontWeight="Bold" Foreground="{ThemeResource AppBackgroundBrush}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{x:Bind Artist}" FontSize="13" Opacity="0.6" MaxLines="1" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource AppTextForegroundBrush}" />
                            </StackPanel>
                        </StackPanel>
 
                        <TextBlock Grid.Column="3" Text="{x:Bind DurationStr}" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.6" FontSize="13" />
                        
                        <TextBlock Grid.Column="4" Text="{x:Bind Album}" VerticalAlignment="Center" Margin="12,0" Opacity="0.6" MaxLines="1" TextTrimming="CharacterEllipsis" FontSize="13" />
                        
                        <TextBlock Grid.Column="5" Text="{x:Bind Genre}" VerticalAlignment="Center" Margin="12,0" Opacity="0.6" MaxLines="1" TextTrimming="CharacterEllipsis" FontSize="13" />
                        
                        <TextBlock Grid.Column="6" Text="{x:Bind Year}" VerticalAlignment="Center" Margin="12,0" Opacity="0.6" HorizontalAlignment="Right" FontSize="13" />

                        <ToggleButton Grid.Column="7" Click="OnFavoriteClicked" IsChecked="{x:Bind IsFavorite, Mode=OneWay}" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEB51;" FontSize="16" Foreground="{ThemeResource AppTextForegroundBrush}" />
                        </ToggleButton>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="True" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
    </Grid>
</Page>
