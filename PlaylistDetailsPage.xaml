<Page
    x:Class="Opal.PlaylistDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Opal"
    xmlns:converters="using:Opal.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:ExplicitStatusConverter x:Key="ExplicitStatusConverter" />
    </Page.Resources>

    <Grid Background="{ThemeResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="20,20,20,10" Orientation="Horizontal">
            <Button Click="OnBackClicked" Style="{StaticResource NavigationBackButtonNormalStyle}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="PlaylistTitle" Style="{StaticResource HeaderTextBlockStyle}" Text="Playlist Name" />
                <TextBlock x:Name="PlaylistDetails" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" Text="0 Songs · 0:00" />
            </StackPanel>
        </StackPanel>

        <!-- Song List -->
        <ListView x:Name="TracksListView" 
                  Grid.Row="1"
                  ItemsSource="{x:Bind Songs}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="OnTrackClicked"
                  CanDragItems="True"
                  CanReorderItems="True"
                  AllowDrop="True"
                  DragItemsCompleted="OnDragItemsCompleted">
            
            <ListView.Header>
                <Grid Padding="12,0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="60" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="#" FontWeight="SemiBold" Opacity="0.6" HorizontalAlignment="Center" />
                    <TextBlock Grid.Column="1" Text="Title" FontWeight="SemiBold" Opacity="0.6" Margin="12,0" />
                    <TextBlock Grid.Column="2" Text="Album" FontWeight="SemiBold" Opacity="0.6" Margin="12,0" />
                    <SymbolIcon Grid.Column="3" Symbol="Clock" Opacity="0.6" HorizontalAlignment="Right" />
                </Grid>
            </ListView.Header>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,2" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Song">
                    <Grid Height="56" Padding="12,0" Background="Transparent" Name="TrackGrid">
                        <Grid.ContextFlyout>
                            <MenuFlyout Opening="OnTrackContextOpening">
                                <MenuFlyoutItem Text="Play" Click="OnPlayMenuClicked" Icon="Play" />
                                <MenuFlyoutItem Text="Add to Queue" Click="OnAddQueueMenuClicked" Icon="Add" />
                                <MenuFlyoutSeparator />
                                <MenuFlyoutItem Name="RemoveFromPlaylistMenu" Text="Remove from this Playlist" Click="OnRemoveMenuClicked" Icon="Delete" />
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                        
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{x:Bind PlaylistIndex}" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.6" />
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Bind Title}" MaxLines="1" TextTrimming="CharacterEllipsis" FontSize="15" FontWeight="Medium" />
                                <Border Margin="6,0,0,0" Background="#55FFFFFF" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Visibility="{x:Bind ExplicitStatus, Converter={StaticResource ExplicitStatusConverter}}">
                                    <TextBlock Text="E" FontSize="10" FontWeight="Bold" />
                                </Border>
                            </StackPanel>
                            <TextBlock Text="{x:Bind Artist}" FontSize="13" Opacity="0.6" MaxLines="1" TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="{x:Bind Album}" VerticalAlignment="Center" Margin="12,0" Opacity="0.6" MaxLines="1" TextTrimming="CharacterEllipsis" />
                        
                        <TextBlock Grid.Column="3" Text="{x:Bind DurationStr}" VerticalAlignment="Center" HorizontalAlignment="Right" Opacity="0.6" />
                        
                        <Button Grid.Column="4" Visibility="Collapsed" /> <!-- Spacer -->
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="True" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
    </Grid>
</Page>
