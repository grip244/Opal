<Page
    x:Class="Opal.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Opal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DeviceStates">
                <VisualState x:Name="XboxState">
                    <VisualState.Setters>
                        <Setter Target="SettingsHeader.FontSize" Value="20" />
                        <Setter Target="AccountUserText.FontSize" Value="18" />
                        <Setter Target="AvatarContainer.Width" Value="48" />
                        <Setter Target="AvatarContainer.Height" Value="48" />
                        <Setter Target="RootScroll.Padding" Value="48,40,48,48" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <ScrollViewer x:Name="RootScroll" Padding="40" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="32" MaxWidth="800" HorizontalAlignment="Left">
                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromVerticalOffset="20" />
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>
                <TextBlock x:Name="SettingsHeader" Text="Settings" FontSize="28" FontWeight="Bold" Margin="0,0,0,12" Foreground="{ThemeResource AppTextForegroundBrush}"/>


                <StackPanel Spacing="16">
                    <TextBlock Text="Playback" FontSize="20" FontWeight="SemiBold"/>
                    <Border Background="{ThemeResource AppControlBackgroundBrush}" Padding="24" CornerRadius="8">
                        <StackPanel Spacing="12">
                            <ToggleSwitch x:Name="AutoPlayToggle" Header="Auto-Play" 
                                          Toggled="OnAutoPlayToggled" IsOn="True" UseSystemFocusVisuals="True"/>
                            <TextBlock Text="Automatically fetch and play random songs when the current queue ends." 
                                       FontSize="12" Opacity="0.5" TextWrapping="Wrap" Margin="0,-4,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="16">
                    <TextBlock Text="Account" FontSize="20" FontWeight="SemiBold" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                    <Border Background="{ThemeResource AppControlBackgroundBrush}" BorderBrush="{ThemeResource AppControlBorderBrush}" BorderThickness="1" Padding="24" CornerRadius="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <!-- User Avatar Icon -->
                            <Border x:Name="AvatarContainer" Width="64" Height="64" CornerRadius="32" Background="{ThemeResource SystemAccentColor}" Margin="0,0,20,0">
                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE77B;" FontSize="32" Foreground="White" />
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock x:Name="AccountUserText" FontSize="22" FontWeight="Bold" Foreground="{ThemeResource AppTextForegroundBrush}" />
                                <StackPanel Orientation="Horizontal" Spacing="6" Opacity="0.6">
                                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE83B;" FontSize="12" VerticalAlignment="Center" Foreground="{ThemeResource AppTextForegroundBrush}" />
                                    <TextBlock x:Name="AccountServerText" FontSize="14" VerticalAlignment="Center" Foreground="{ThemeResource AppTextForegroundBrush}" />
                                </StackPanel>
                            </StackPanel>

                            <Button Grid.Column="2" Content="Logout" Click="OnLogoutClicked" 
                                    Foreground="#FFFF4444" BorderBrush="#33FF4444" BorderThickness="1"
                                    Background="Transparent" Padding="24,8" CornerRadius="20" 
                                    VerticalAlignment="Center" UseSystemFocusVisuals="True"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- 3.1: Controller Reference Card -->
                <StackPanel Spacing="16">
                    <TextBlock Text="Controller" FontSize="20" FontWeight="SemiBold" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                    <Border Background="{ThemeResource AppControlBackgroundBrush}" BorderBrush="{ThemeResource AppControlBorderBrush}" BorderThickness="1" Padding="24" CornerRadius="12">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Xbox Button Mappings" FontSize="14" FontWeight="SemiBold" Opacity="0.8" Margin="0,0,0,8" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <!-- Row headers -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="LB" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="Previous Track" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="RB" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="Next Track" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="X" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="Play / Pause" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Y" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="Focus Search Box" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                                <TextBlock Grid.Row="4" Grid.Column="0" Text="View (⧉)" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="Toggle Now Playing" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                                <TextBlock Grid.Row="5" Grid.Column="0" Text="LT" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="5" Grid.Column="1" Text="Volume Down" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                                <TextBlock Grid.Row="6" Grid.Column="0" Text="RT" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="6" Grid.Column="1" Text="Volume Up" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                                <TextBlock Grid.Row="7" Grid.Column="0" Text="Menu (≡)" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" Margin="0,4"/>
                                <TextBlock Grid.Row="7" Grid.Column="1" Text="Advance Field / Submit" Opacity="0.8" Margin="0,4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                            </Grid>
                            <TextBlock Text="Button remapping coming in v1.2.0." FontSize="11" Opacity="0.4" Margin="0,8,0,0" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="16">
                    <TextBlock Text="Legal" FontSize="20" FontWeight="SemiBold"/>
                    <Border Background="{ThemeResource AppControlBackgroundBrush}" Padding="24" CornerRadius="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <HyperlinkButton Content="Privacy Policy" NavigateUri="https://darademsc.ddns.net/#privacy" Padding="0" UseSystemFocusVisuals="True"/>
                            <TextBlock Text="•" VerticalAlignment="Center" Opacity="0.4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                            <HyperlinkButton Content="Terms of Service" NavigateUri="https://darademsc.ddns.net/#tos" Padding="0" UseSystemFocusVisuals="True"/>
                            <TextBlock Text="•" VerticalAlignment="Center" Opacity="0.4" Foreground="{ThemeResource AppTextForegroundBrush}"/>
                            <HyperlinkButton Content="GitHub Repository" NavigateUri="https://github.com/grip244/Opal" Padding="0" UseSystemFocusVisuals="True"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                <TextBlock Text="Opal v1.1.6" Opacity="0.4" HorizontalAlignment="Center" Margin="0,40,0,20" Foreground="{ThemeResource AppTextForegroundBrush}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
